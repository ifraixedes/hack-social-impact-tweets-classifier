(ns classifier.config.credentials.twitter)

(def social-impact-keys {:api-key "twitter application key"
                         :api-secret "twitter application secret"
                         :access-token "twitter application access token"
                         :access-token-secret "twitter application access secret"})
